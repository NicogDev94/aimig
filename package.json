{
  "name": "aimig",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "benchmark": "vitest run --silent=false packages/core/test/benchmark-report.test.ts",
    "demo": "tsc -b && node packages/core/dist/src/node/demo-cli.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "yaml": "^2.6.1"
  },
  "engines": {
    "node": ">=20"
  }
}
