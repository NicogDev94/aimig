# @aimig/web

Placeholder. The Research UI is **PR6**, which does not start automatically:
it is gated on the Validation Gate described in `docs/milestone-0.md` §7.

This package exists now only to declare the workspace boundary. It carries no
dependencies on purpose — the reasoning engine in `@aimig/core` must be fully
demonstrable without any UI (`yarn demo`, from PR5).
